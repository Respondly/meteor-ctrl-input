@import 'core.import'


.c-select
  background rgba(white, 0.8)
  border-color rgba(black, 0.3)
  appearance none
  outline none
  line-height normal
  padding 6px 10px
  min-height 32px

  &.c-disabled
    opacity 0.3
